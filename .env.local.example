# Public site URL, used for sitemap/robots/Open Graph — set to the real deployed domain.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Required only once the AI job-fit feature (app/api/fit) is built — not yet in use.
# ANTHROPIC_API_KEY=
